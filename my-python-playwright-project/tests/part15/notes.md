
# Part 15: Inspect Selectors with DevTools

Use Chrome DevTools to inspect HTML and validate selector paths.

Steps:
1. Right-click > Inspect
2. Use `#id`, `.class`, or text selector in Playwright
3. Test in `playwright codegen` or trace

This test fills a username field on a real form.
